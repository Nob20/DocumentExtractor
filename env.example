# OpenAI API Configuration (Optional)
# To enable OpenAI-enhanced extraction, copy this file to .env.local and add your API key

# Your OpenAI API key from https://platform.openai.com/api-keys
REACT_APP_OPENAI_API_KEY=sk-your-api-key-here

# OpenAI model to use (gpt-4 recommended, gpt-3.5-turbo for faster/cheaper)
REACT_APP_OPENAI_MODEL=gpt-4

# Instructions:
# 1. Copy this file: cp env.example .env.local
# 2. Replace "sk-your-api-key-here" with your actual OpenAI API key
# 3. Restart the development server: npm start
# 4. The OpenAI toggle will appear automatically in the UI

# Security Notes:
# - NEVER commit .env.local to version control
# - Keep your API key secure
# - .env.local is already in .gitignore
# - API keys are only stored locally, never sent to any backend

